#!/usr/bin/env bash

source include

file_benchmark="${project_root}/data/BenchmarkEnsemblMouse.fa.gz"

msg "Seq-Ana benchmark."
stack --work-dir "$prod_dir" exec seq-ana -- summarize -a DNAIUPAC "$file_benchmark"  +RTS -s || exit 1
