#!/usr/bin/env bash

source include
./build || exit 1

file_benchmark=../data/BenchmarkEnsemblMouse.fa.gz

msg "EvoMod-Seq benchmark."
stack exec evomod-seq -- summarize -a DNA_IUPAC $file_benchmark  +RTS -s || exit 1
